import { ImageEditingDirective } from './image-editing.directive';

describe('ImageEditingDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageEditingDirective();
    expect(directive).toBeTruthy();
  });
});
